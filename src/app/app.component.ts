import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',  // Specify then name of the template file for this particular component (app)
  styleUrls: ['./app.component.scss']   // Specify then name of the style file for this particular component (app)  
})
export class AppComponent {
  title = 'app';
}
